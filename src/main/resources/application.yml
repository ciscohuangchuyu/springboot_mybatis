server:
  port: 8080
logging:
  config: classpath:logback-boot.xml
  path: logs
  level:
      root: info
spring:
    datasource:
        name: mysql_test
        type: com.alibaba.druid.pool.DruidDataSource
        #druid相关配置
        druid:
          #监控统计拦截的filters
          filters: stat
          driver-class-name: com.mysql.jdbc.Driver
          #基本属性
          url: jdbc:mysql://47.93.244.103:3306/mytest?useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true
          username: root
          password: root
          #配置初始化大小/最小/最大
          initial-size: 1
          min-idle: 1
          max-active: 20
          #获取连接等待超时时间
          max-wait: 60000
          #间隔多久进行一次检测，检测需要关闭的空闲连接
          time-between-eviction-runs-millis: 60000
          #一个连接在池中最小生存的时间
          min-evictable-idle-time-millis: 300000
          validation-query: SELECT 'x'
          test-while-idle: true
          test-on-borrow: false
          test-on-return: false
          #打开PSCache，并指定每个连接上PSCache的大小。oracle设为true，mysql设为false。分库分表较多推荐设置为false
          pool-prepared-statements: false
          max-pool-prepared-statement-per-connection-size: 20
    application:
      name: testBiz


# 该配置节点为独立的节点，有很多同学容易将这个配置放在spring的节点下，导致配置无法被识别
mybatis:
  config-location: classpath:spring-mybatis.xml
  mapper-locations: classpath*:com/example/mybatisdemo/persistence/**/mapping/*.xml  #注意：一定要对应mapper映射xml文件的所在路径
  type-aliases-package: com.example.mybatisdemo.persistence.**.model  # 注意：对应实体类的路径
#  configuration:
#    #开启数据库驼峰读取
#    map-underscore-to-camel-case: true
#    #打印sql语句,调试用
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
#    #全局映射器启用缓存
#    cache-enabled: false
#    #查询时，关闭关联对象即时加载以提高性能
#    lazy-loading-enabled: true
#    #设置关联对象加载的形态，此处为按需加载字段(加载字段由SQL指 定)，不会加载关联表的所有字段，以提高性能
#    aggressive-lazy-loading: false
#    #对于未知的SQL查询，允许返回不同的结果集以达到通用的效果
#    multiple-result-sets-enabled: true
#    #允许使用列标签代替列名
#    use-column-label: true
#    #允许使用自定义的主键值(比如由程序生成的UUID 32位编码作为键值)，数据表的PK生成策略将被覆盖
#    use-generated-keys: true
#    #给予被嵌套的resultMap以字段-属性的映射支持
#    auto-mapping-behavior: full
#    #数据库超过25000秒仍未响应则超时
#    default-statement-timeout: 25000




